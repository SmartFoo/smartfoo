# Package com.smartfoo.android.core.content

Android content and preferences abstractions. `FooPreferences` wraps `SharedPreferences` with convenient per-file accessors for app-level and user-level preference files, integrates Android's `BackupManager` so changes are automatically flagged for cloud backup, and provides typed getters and setters for common preference value types.
