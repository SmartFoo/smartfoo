# Package com.smartfoo.android.core.annotations

Custom compile-time annotations for the SmartFoo library. Currently provides `NonNullNonEmpty`, a `@Size(min=1)` meta-annotation that marks strings and collections as required to be both non-null and non-empty, complementing the standard AndroidX nullability annotations.
