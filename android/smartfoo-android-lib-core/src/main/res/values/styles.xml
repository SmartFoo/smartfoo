<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="FooTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/FooColorPrimaryBackground</item>
        <item name="colorPrimaryDark">@color/FooColorPrimaryBackgroundDark</item>
        <item name="colorAccent">@color/FooColorAccent</item>
    </style>

    <style name="FooTheme.ToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <!--
        <item name="android:titleTextColor">@color/FooColorPrimaryText</item>
        -->
    </style>

    <style name="FooTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--
        <!- - android:textColorPrimary is the color of the title text in the Toolbar  - ->
        <item name="android:textColorPrimary">@android:color/holo_blue_light</item>
        <!- - actionMenuTextColor is the color of the text of action (menu) items  - ->
        <item name="actionMenuTextColor">@android:color/holo_green_light</item>
        <!- - Tints the input fields like checkboxes and text fields - ->
        <item name="colorAccent">@color/cursorAccent</item>
        <!- - Applies to views in their normal state. - ->
        <item name="colorControlNormal">@color/controlNormal</item>
        <!- - Applies to views in their activated state (i.e checked or switches) - ->
        <item name="colorControlActivated">@color/controlActivated</item>
        <!- - Applied to framework control highlights (i.e ripples or list selectors) - ->
        <item name="colorControlHighlight">@color/controlActivated</item>

        <!- - Enable these below if you want clicking icons to trigger a ripple effect - ->
        <!- -
        <item name="selectableItemBackground">?android:selectableItemBackground</item>
        <item name="selectableItemBackgroundBorderless">?android:selectableItemBackground</item>
        - ->
        -->
    </style>

    <style name="Theme.Alfred.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="FooTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />

</resources>
